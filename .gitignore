target/
build/
node_modules/
.wrangler/
.worktrees/
*.png
*.jpg
*.rlib

# Build outputs
dist/
crates/spa/dist/

# Local dev artifacts
.serve/
.playwright-mcp/
package-lock.json
.dev.vars
.dev.vars.local
.env
.env.local

# Playwright run artifacts + the e2e shared-session cookie cache
# (tests/e2e/fixtures.ts) — regenerated every run, machine/run-specific.
tests/test-results/
tests/playwright-report/
tests/.auth-cache.json

# Editor / tooling local config — only the runtime artefacts under
# .claude/ are ignored. Everything else (commands, settings, etc.)
# is tracked.
.claude/worktrees/
.claude/scheduled_tasks.lock
wrangler.dev-local.toml
